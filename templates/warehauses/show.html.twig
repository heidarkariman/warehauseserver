{% extends 'base.html.twig' %}

{% block title %}Warehauses{% endblock %}

{% block body %}
    <h1>Warehauses</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ warehause.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ warehause.name }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ warehause.address }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_warehauses_index') }}">back to list</a>

    <a href="{{ path('app_warehauses_edit', {'id': warehause.id}) }}">edit</a>

    {{ include('warehauses/_delete_form.html.twig') }}
{% endblock %}
