<form method="post" action="{{ path('app_warehauses_delete', {'id': warehause.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ warehause.id) }}">
    <button class="btn">Delete</button>
</form>
